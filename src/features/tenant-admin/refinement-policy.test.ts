import {describe,expect,it} from "vitest";
import {adminSectionActive,safeInlineButtonUrl,TENANT_GAME_SECTIONS,TENANT_WALLET_FIELDS} from "./refinement-policy";
import {engagementReady,safeTaskDestination} from "../tasks/engine";
describe("refined Admin policy",()=>{it("keeps nested desktop and mobile sections active",()=>{const base="/miniapp/admin";expect(adminSectionActive("/miniapp/admin/tasks",base,"/tasks")).toBe(true);expect(adminSectionActive("/miniapp/admin/wallet/transactions",base,"/wallet")).toBe(true);expect(adminSectionActive("/miniapp/admin/games/memory-match",base,"/games")).toBe(true);expect(adminSectionActive("/miniapp/admin/users",base,"")).toBe(false)});it("limits Wallet settings to six business fields",()=>{expect(TENANT_WALLET_FIELDS).toEqual(["currency","pointsPerDollar","minimumConversionPoints","conversionFeePercent","minimumWithdrawal","maximumWithdrawal"])});it("has exactly the three platform games",()=>{expect(TENANT_GAME_SECTIONS).toHaveLength(3)});it("rejects unsafe inline and task URLs",()=>{expect(safeInlineButtonUrl("https://t.me/example")).toBe(true);expect(safeInlineButtonUrl("javascript:alert(1)")).toBe(false);expect(safeInlineButtonUrl("data:text/html,x")).toBe(false);expect(safeTaskDestination("https://example.com/task")).toBeTruthy();expect(safeTaskDestination("http://example.com")).toBeNull()});it("enforces the five-second gate from server dates",()=>{const opened=new Date("2026-01-01T00:00:00Z");expect(engagementReady(opened,5,new Date("2026-01-01T00:00:04.999Z"))).toBe(false);expect(engagementReady(opened,5,new Date("2026-01-01T00:00:05Z"))).toBe(true)})});
