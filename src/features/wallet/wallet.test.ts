import { Prisma } from "@prisma/client";import { afterEach,describe,expect,it } from "vitest";import { conversionAmounts,formatUsd,maskDestination,withdrawalFee } from "./money";import { decryptDestination,encryptDestination } from "./encryption";
describe("wallet arithmetic",()=>{it("formats normal and tiny Decimal rewards",()=>{expect(formatUsd("1.2")).toBe("$1.20");expect(formatUsd("0.000125")).toBe("$0.000125");});it("converts with integer micro-units and rounds down",()=>{const r=conversionAmounts(1500,1000,250);expect(r.gross.toFixed(6)).toBe("1.500000");expect(r.fee.toFixed(6)).toBe("0.037500");expect(r.net.toFixed(6)).toBe("1.462500");});it("rounds withdrawal fees upward to supported precision",()=>expect(withdrawalFee(new Prisma.Decimal("10"),new Prisma.Decimal("0.1"),125).toFixed(6)).toBe("0.225000"));it("masks payout destinations",()=>expect(maskDestination("TLongWalletAddress1234")).toBe("TL••••••••34"));});
describe("payout encryption",()=>{afterEach(()=>delete process.env.WALLET_ENCRYPTION_KEY);it("round trips AES-GCM without exposing plaintext",()=>{process.env.WALLET_ENCRYPTION_KEY="test-key-that-is-at-least-thirty-two-characters";const encrypted=encryptDestination("account-123456");expect(encrypted).not.toContain("account");expect(decryptDestination(encrypted)).toBe("account-123456");});});
