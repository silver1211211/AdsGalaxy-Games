import { describe,expect,it } from "vitest";import { engagementReady,hashTaskAnswer,proofFileAllowed,safeTaskDestination,taskPeriodKey,verificationLabel,verifyTaskAnswer } from "./engine";
describe("task safety engine",()=>{it("calculates stable repeat periods",()=>{const d=new Date("2026-07-27T12:00:00Z");expect(taskPeriodKey("ONCE",d)).toBe("once");expect(taskPeriodKey("DAILY",d)).toBe("2026-07-27");expect(taskPeriodKey("WEEKLY",d)).toBe("week:2026-07-27");});it("uses server timestamps for engagement",()=>{const opened=new Date("2026-01-01T00:00:00Z");expect(engagementReady(opened,20,new Date("2026-01-01T00:00:19Z"))).toBe(false);expect(engagementReady(opened,20,new Date("2026-01-01T00:00:20Z"))).toBe(true);});it("hashes and verifies normalized answers",()=>{const a=hashTaskAnswer("  Galaxy CODE ");expect(verifyTaskAnswer("galaxy code",a.salt,a.hash)).toBe(true);expect(verifyTaskAnswer("wrong",a.salt,a.hash)).toBe(false);});it("allows only safe HTTPS destinations",()=>{expect(safeTaskDestination("https://t.me/example")).toContain("https://");expect(safeTaskDestination("javascript:alert(1)")).toBeNull();expect(safeTaskDestination("http://example.com")).toBeNull();});it("uses truthful verification labels",()=>{expect(verificationLabel("REINFORCED_SELF_CONFIRMATION","SELF_CONFIRMED")).toContain("not independently verifiable");expect(verificationLabel("MANUAL_REVIEW","APPROVED")).toBe("Verified by Admin");});it("validates proof files",()=>{expect(proofFileAllowed({mime:"image/png",size:100})).toBe(true);expect(proofFileAllowed({mime:"application/javascript",size:100})).toBe(false);expect(proofFileAllowed({mime:"image/png",size:6e6})).toBe(false);});});
