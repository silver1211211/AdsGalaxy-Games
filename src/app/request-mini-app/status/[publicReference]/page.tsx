import type{Metadata}from"next";import Link from"next/link";import{notFound}from"next/navigation";import{ArrowLeft}from"lucide-react";import{getSession}from"@/lib/session";import{applicantRequest}from"@/features/mini-app-requests/server";import{RequestDetail}from"@/components/public/request-detail";
export const metadata:Metadata={title:"Request Details",robots:{index:false,follow:false}};
export default async function Detail({params}:{params:Promise<{publicReference:string}>}){const session=await getSession();if(!session)notFound();const{publicReference}=await params,item=await applicantRequest(session.userId,publicReference);if(!item)notFound();return <main className="mx-auto min-h-dvh max-w-3xl px-4 py-6"><Link href="/request-mini-app/status" className="game-icon-button"><ArrowLeft/></Link><div className="mt-8"><RequestDetail item={item}/></div></main>}
