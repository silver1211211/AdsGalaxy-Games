import Link from "next/link";import { redirect } from "next/navigation";import { ArrowLeft } from "lucide-react";import { requireAdmin } from "@/lib/session";import { TapSettingsForm } from "@/components/admin/tap-settings-form";
export default async function TapSettingsPage(){try{await requireAdmin();}catch{redirect("/games");}return <main className="mx-auto min-h-dvh max-w-[980px] px-4 py-6"><Link href="/admin/game-settings" className="game-icon-button"><ArrowLeft size={18}/></Link><p className="mt-6 text-xs font-extrabold uppercase tracking-[.18em] text-teal-600">Game Settings</p><h1 className="mt-1 text-3xl font-black">Catch Rush</h1><p className="mb-6 mt-2 text-sm text-warm-600">Tenant reward values only. Level mechanics and reward safety are platform-controlled.</p><TapSettingsForm/></main>}
