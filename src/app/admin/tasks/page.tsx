import Link from "next/link";import { redirect } from "next/navigation";import { prisma } from "@/lib/prisma";import { requireAdmin } from "@/lib/session";import { AppShell } from "@/components/layout/app-shell";
export default async function AdminTasksPage(){let a;try{a=await requireAdmin();}catch{redirect("/games");}const[total,active,pending,rewarded]=await Promise.all([prisma.task.count({where:{miniAppId:a.miniAppId}}),prisma.task.count({where:{miniAppId:a.miniAppId,status:"ACTIVE"}}),prisma.taskSubmission.count({where:{miniAppId:a.miniAppId,status:"PENDING_REVIEW"}}),prisma.taskAttempt.count({where:{miniAppId:a.miniAppId,status:"REWARDED"}})]);return <AppShell><main><p className="text-xs font-extrabold uppercase text-teal-600">Administration</p><h1 className="mt-1 text-3xl font-black">Task Center</h1><div className="mt-6 grid grid-cols-2 gap-3 sm:grid-cols-4">{[["Tasks",total],["Active",active],["Pending review",pending],["Rewarded",rewarded]].map(([a,b])=><div key={a} className="rounded-3xl bg-white p-4 shadow-card"><p className="text-xs text-warm-500">{a}</p><p className="mt-1 text-xl font-black">{b}</p></div>)}</div><div className="mt-6 grid gap-4 sm:grid-cols-3">{[["Manage Tasks","/admin/tasks/manage"],["Review Submissions","/admin/tasks/submissions"],["Task Settings","/admin/tasks/settings"]].map(([t,h])=><Link key={h} href={h} className="rounded-4xl bg-white p-5 font-black shadow-card">{t}</Link>)}</div></main></AppShell>}
